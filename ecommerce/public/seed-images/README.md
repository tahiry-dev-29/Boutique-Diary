# Seed Product Images

This directory contains sample product images for database seeding.

## Structure

- `products/` - Product images organized by type
- `customers/` - Customer avatar images
- `banners/` - Banner images for homepage

## Usage

Run `pnpm prisma db seed` to populate the database with these images.
